# Gateway VPN VPS management forwarding. nftables restricts this forwarding to
# the owned wg-mgmt management subnet and approved peer tuple.
net.ipv4.ip_forward = 1
net.ipv6.conf.all.forwarding = 0
net.ipv6.conf.default.forwarding = 0
